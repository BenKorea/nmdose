# ───── 시뮬레이션 모드 설정 ─────
# 1: simulationPACS 사용 / 0: clinicalPACS 사용
ENABLE_SIMULATION=1

# ───── 로그레벨 (선택적) ─────
# 실행 인자 없을 경우 fallback
NMDOSE_LOGLEVEL=debug

# ───── PostgreSQL 슈퍼유저 정보 (DB 생성/초기화 용) ─────
POSTGRESQL_SUPERUSER=postgres
POSTGRESQL_SUPERUSER_PASSWORD=postgres

# ───── NMDOSE 슈퍼유저 (선택적, 별도 관리 가능) ─────
NMDOSE_SUPERUSER=nmdose
NMDOSE_SUPERUSER_PASSWORD=nmdose

# ───── 공통 일반 사용자 (애플리케이션용) ─────
NMDOSE_DB_USER=nmuser
NMDOSE_DB_PASSWORD=nmuser
